# =============================================================================
# Armature - Pod Disruption Budget Template
# =============================================================================
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: armature-api
  namespace: armature
  labels:
    app.kubernetes.io/name: armature-api
    app.kubernetes.io/managed-by: armature-cli
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: armature-api

